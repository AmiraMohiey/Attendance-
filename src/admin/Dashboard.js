import React from 'react';
import { Card, CardHeader, CardText } from 'material-ui/Card';

export default () => (
    <Card style={{ margin: '2em' }}>
       
        <CardText>here the admin should add the attendence rules</CardText>
    </Card>
);
